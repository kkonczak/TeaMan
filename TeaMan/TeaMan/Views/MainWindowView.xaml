<Window x:Class="TeaMan.Views.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TeaMan"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <StackPanel Orientation="Vertical">
            <TextBox Text="{Binding Pole1, UpdateSourceTrigger=PropertyChanged}" Width="100" Height="30" />
            <TextBox Text="{Binding Pole1}" Width="100" Height="30" />

            <Label Content="{Binding Pole1}" />
            <Button Content="kliknij mnie" Command="{Binding Command1}" />
        </StackPanel>
    </Grid>
</Window>
